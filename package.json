{
  "name": "orchardeye-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp -R web/. dist",
    "start": "python3 -m http.server 8000 --directory web"
  }
}
